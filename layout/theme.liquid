<!DOCTYPE html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="theme-color" content="" /> 
  <title>{{ page_title }}</title>

  {% if content_for_header contains "previewBarInjector.init();" %}
  <script type="text/javascript">
    var loc = '{{ settings.url }}' + window.location.pathname;
    document.getElementById("main-message").innerHTML = '<p>Preview Mode: ' + loc + '</p>';
  </script>  
  {% elsif content_for_header contains "Shopify.designMode" %}
     <script type="text/javascript">
    var loc = '{{ settings.url }}' + window.location.pathname;
    document.getElementById("main-message").innerHTML = '<p>Preview Mode: ' + loc + '</p>';
  </script>   
  {% else %}
    <script type="text/javascript">
      var loc = '{{ settings.url }}' + window.location.pathname;
      window.location.replace(loc);
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0; URL={{ settings.url }}" />
    </noscript>
  {% endif %}
  
  {{ content_for_header }}
  <style type="text/css">
  .loader,
.loader:after {
  border-radius: 50%;
  width: 10em;
  height: 10em;
}
.loader {
  margin: 60px auto;
  font-size: 10px;
  position: relative;
  text-indent: -9999em;
  border-top: 1.1em solid rgba(50,50,50, 0.2);
  border-right: 1.1em solid rgba(50,50,50, 0.2);
  border-bottom: 1.1em solid rgba(50,50,50, 0.2);
  border-left: 1.1em solid {{ settings.color_primary }};
  -webkit-transform: translateZ(0);
  -ms-transform: translateZ(0);
  transform: translateZ(0);
  -webkit-animation: load8 1.1s infinite linear;
  animation: load8 1.1s infinite linear;
}
@-webkit-keyframes load8 {
  0% {
    -webkit-transform: rotate(0deg);
    transform: rotate(0deg);
  }
  100% {
    -webkit-transform: rotate(360deg);
    transform: rotate(360deg);
  }
}
@keyframes load8 {
  0% {
    -webkit-transform: rotate(0deg);
    transform: rotate(0deg);
  }
  100% {
    -webkit-transform: rotate(360deg);
    transform: rotate(360deg);
  }
}

  </style>
</head>
<body>
  <main id="main" role="main" tabindex="-1">
    <div id="main-message"></div>
    <div class="loader">Loading...</div>
    {{ content_for_layout }}
  </main>
</body>
</html>